name: legacy-source-snapshot

on:
  push:
    branches:
      - analysis/legacy-forge-1.19.2
    paths:
      - .github/workflows/legacy-source-snapshot.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Archive source tree
        run: git archive --format=tar.gz --output=legacy-voxy-source.tar.gz HEAD
      - name: Upload source tree
        uses: actions/upload-artifact@v4
        with:
          name: legacy-voxy-source
          path: legacy-voxy-source.tar.gz
          if-no-files-found: error
